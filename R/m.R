#' Display spatial data from a list or a \code{qmap} object.
#'
#' This funtion genarates a call to \code{ggplot2} and builds the quick map
#' visualization.
#' 
#' @param mapdata A list of spatial objects or a \code{qmap} object.
#' @return Function displays a map from the input \code{mapdata} paramter
#' 
#' @import ggplot2
#' @export
#' 
#' @examples
#' data(lakes)
#' m(list(exampleLake,exampleElev))
m<-function(mapdata){
  
}